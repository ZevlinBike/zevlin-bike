Draft check registry

This folder contains the check registry and light stubs. Real checks that query Supabase or hit internal API routes should run as server actions when we wire the monitor into the Next.js app. For now, these placeholders outline coverage tied to our domain.

